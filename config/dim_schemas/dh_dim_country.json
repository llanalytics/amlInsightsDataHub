{
  "required": ["country_code_2", "country_code_3", "country_name"],
  "properties": {
    "country_code_2": {
      "type": "string",
      "maxLength": 2,
      "dq": {
        "not_null": true,
        "regex": "^[A-Z]{2}$"
      }
    },
    "country_name": {
      "type": "string",
      "maxLength": 120,
      "dq": {
        "not_null": true
      }
    },
    "country_code_3": {
      "type": "string",
      "maxLength": 3,
      "dq": {
        "not_null": true,
        "regex": "^[A-Z]{3}$"
      }
    }
  },
  "additionalProperties": false
}
